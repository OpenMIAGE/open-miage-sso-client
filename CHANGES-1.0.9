  * client Changes
  * improve reconnection display and process : stop to regulary ask to server
    if it's connected, it's now iframe that call a trigger function immediatly
    after reconnection. For display, show now a notification with loader during
    reconnection.
  * remove embeded mode because it's useless
  * during reconnection process, redirect to login only in opening reconnection
    form frame case
 
 * adds
  * N/A

 * libraries Changes
  * N/A