* client Changes
  * bugfix login in progress from OpenM-Id.proxy when reload root page : authentification transaction conflict
    -> in this case, all is re-init
 
 * adds
  * 

 * libraries Changes
  * util v1.0.23