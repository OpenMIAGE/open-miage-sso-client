* client Changes
  * abusive rename not fixed for 1.0, is now fixed : OpenM_SSOClientSession::getAll()
    is now fixed to OpenM_SSOClientSession::getProperties()
    Impacted: OpenM_SSOClientPoolSessionImpl, OpenM_SSOClientSessionImpl (see documentation)